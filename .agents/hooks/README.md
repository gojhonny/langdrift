# Hooks

Reserved for hook implementations after an owner approves them and they are verified. Nothing here is implemented. Moving a script into this directory would not make its event format tool-independent. Cursor registration, when it exists, belongs in `.cursor/hooks.json` and must point at a real implementation.
