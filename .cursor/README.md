# Cursor

Cursor-specific configuration for this repository. It is not a second project policy.

`hooks.json` registers no hooks. No hook is active. `.cursor/rules/` is reserved for Cursor activation that points at a canonical project source. Nothing there is implemented.

This directory does not prove that a hook ran or that the workflow is effective. The previous shell guard, edit feedback hook, and `langdrift-reviewer` were already absent at the preflight commit and were not restored.
