# .drifts

Experimental structure draft for `@langdrift/sdk` and LangDrift core (`packages/core`, published as `@langdrift/setup`) tests.

This tree is a draft of the local Vision, Loop, and Evidence layout those packages exercise. It is not a production data store, and it is not a settled product contract.
