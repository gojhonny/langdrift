# Tickets

Authored work records, one file per ticket: `<id>-<slug>.ticket.md`. Each ticket references its spec. Planning produces them through an explicit `to-tickets` invocation after the spec is approved. No ticket has been created for the first cycle.
