# Specs

Authored contracts, one file per spec: `<id>-<slug>.spec.md`. Planning produces them through an explicit `to-spec` invocation. The field meanings are in `.agents/workflow/README.md`. No spec has been created for the first cycle.
